def insertionSort1(n, arr):
    # Write your code here
    
    for i in range(n-1):
        key=arr[i+1]
        for j in range(i , -1 , -1):
            if arr[j] > key:
                arr[arr.index(key)]=arr[j]
                print(*arr)
                arr[j]=key

    print (*arr)
    
     
